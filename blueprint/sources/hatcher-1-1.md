# Hatcher §1.1, Basic Constructions

Numbered results in [Hatcher](hatcher.md) §1.1, pages 25–38. This is the
section the roadmap decomposes into formalization nodes. Printed page numbers;
the PDF's page *n* is printed page *n − 9*.

Statements are paraphrased to a line. Read the actual text in your own copy.

## Paths and homotopy (25–29)

| Result | Page | Paraphrase |
| --- | --- | --- |
| Example 1.1 | 25 | Linear homotopy: two paths in ℝⁿ with common endpoints are homotopic through `(1-t)f₀ + t f₁`. |
| Proposition 1.2 | 26 | Homotopy of paths with fixed endpoints is an equivalence relation. |
| Proposition 1.3 | 26 | `π₁(X, x₀)` is a group under `[f][g] = [f·g]`. |
| Example 1.4 | 27 | A convex `X ⊆ ℝⁿ` has trivial `π₁`. |
| Proposition 1.5 | 28 | Change of basepoint `βₕ : π₁(X, x₁) → π₁(X, x₀)` along a path `h` is an isomorphism. |
| Proposition 1.6 | 28 | `X` is simply connected iff every pair of points is joined by a unique path-homotopy class. |

## The fundamental group of the circle (29–33)

| Result | Page | Paraphrase |
| --- | --- | --- |
| **Theorem 1.7** | 29 | `π₁(S¹)` is infinite cyclic, generated by the class of `ω(s) = (cos 2πs, sin 2πs)` based at `(1,0)`. |
| Theorem 1.8 | 31 | Fundamental theorem of algebra: every nonconstant `p ∈ ℂ[X]` has a root. |
| Theorem 1.9 | 31 | Brouwer for the disc: every continuous `h : D² → D²` has a fixed point. |
| Theorem 1.10 | 32 | Borsuk–Ulam for `S²`: every continuous `f : S² → ℝ²` identifies some antipodal pair. |
| Corollary 1.11 | 33 | If `S²` is a union of three closed sets, one of them contains an antipodal pair. |

Hatcher proves Theorem 1.7 from two covering-space facts about `p : ℝ → S¹`,
`p(s) = (cos 2πs, sin 2πs)`, stated on page 29 as (a) unique path lifting and
(b) unique homotopy lifting, both then deduced from the general statement (c)
for a covering space `p : X̃ → X` on page 30. Mathlib supplies (a), (b), and (c);
see the roadmap nodes for the exact declarations.

## Induced homomorphisms (34–38)

| Result | Page | Paraphrase |
| --- | --- | --- |
| Proposition 1.12 | 34 | `π₁(X × Y) ≅ π₁(X) × π₁(Y)` for path-connected `X`, `Y`. |
| Example 1.13 | 34 | The torus: `π₁(S¹ × S¹) ≅ ℤ × ℤ`. |
| Proposition 1.14 | 35 | `π₁(Sⁿ) = 0` for `n ≥ 2`. |
| Lemma 1.15 | 35 | A loop in a union of path-connected open sets, pairwise meeting in path-connected sets, factors through finitely many of them. |
| Corollary 1.16 | 36 | `ℝ²` is not homeomorphic to `ℝⁿ` for `n ≠ 2`. |

## Prior art in the pinned Mathlib

Checked against the Mathlib revision this project pins (`v4.31.0`,
`fabf563a7c95a166b8d7b6efca11c8b4dc9d911f`).

Available and reusable:

- `Path.Homotopic`, `FundamentalGroup`, `FundamentalGroupoid` —
  `Mathlib/AlgebraicTopology/FundamentalGroupoid/`. Propositions 1.2, 1.3,
  and 1.5 are already there.
- `SimplyConnectedSpace` — `FundamentalGroupoid/SimplyConnected.lean`,
  covering Proposition 1.6.
- `FundamentalGroupoid.prodToProdTop` — `FundamentalGroupoid/Product.lean`,
  covering Proposition 1.12 at groupoid level.
- `IsCoveringMap.liftPath`, `liftHomotopy`, `monodromy`, `monodromy_theorem` —
  `Mathlib/Topology/Homotopy/Lifting.lean`. These are Hatcher's (a), (b), (c).
- `AddCircle.isCoveringMap_coe : IsCoveringMap ((↑) : 𝕜 → AddCircle p)` —
  `Mathlib/Topology/Covering/AddCircle.lean`. Hatcher's `p : ℝ → S¹`, in
  `AddCircle` coordinates.
- `Complex.isAlgClosed` — `Mathlib/Analysis/Complex/Polynomial/Basic.lean`.
  Theorem 1.8's *statement* is upstream, proved by Liouville rather than by
  `π₁`. Formalizing Hatcher's route gives a second proof, not a new result.

Absent, and therefore the work:

- `π₁(S¹) ≅ ℤ` (Theorem 1.7). No declaration in the pinned revision computes
  it, though every input above exists.
- Brouwer's fixed point theorem in any dimension (Theorem 1.9).
- Borsuk–Ulam in any dimension (Theorem 1.10) and Corollary 1.11.
- `π₁(Sⁿ) = 0` for `n ≥ 2` (Proposition 1.14) and Corollary 1.16.

## Open source questions

- Hatcher's `S¹` is the unit circle in `ℝ²`. Mathlib's covering-space result is
  stated for `AddCircle p`. Whether the project states Theorem 1.7 for
  `AddCircle (1 : ℝ)`, for `Metric.sphere (0 : ℂ) 1`, or for `Circle`, and how
  the transport between them is discharged, is not yet decided.
- Proposition 1.14 and Corollary 1.16 need Lemma 1.15, which is the
  path-connected-cover argument that §1.2 generalizes into van Kampen. Whether
  to formalize Lemma 1.15 standalone or defer it to the §1.2 work is open.
